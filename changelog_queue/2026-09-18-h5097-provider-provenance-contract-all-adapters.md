- **Provider provenance contract generalized to every adapter (H5097, OxAlpha (opencode/z-ai/glm-5.3-flash), 18-09-2026):** the H5071 requested-vs-actual provenance repair (PR #227) covered only Mock and DeepSeek; the other six adapters (`openai`, `google`, `anthropic`, `openrouter`, `local`, `ollama`) completed or failed with `last_call_provenance()` still reporting `not_started`, so [`provider.log.jsonl`](https://github.com/gasyoun/RuWritingStyles/blob/main/src/ruwritingstyles/provider_log.py) omitted their outcome block entirely — an adapter could complete without a truthful outcome record. Now every adapter runs one shared contract (`BaseProvider._record_call_provenance`): truthful requested/actual identity, `completed_direct` with `usage_available` read off the actual response wire format (OpenAI/Gemini/Anthropic/Ollama/OpenRouter/Local shapes each handled), and a new `failed_direct` outcome (additive to the H5071 vocabulary, now a validated `ProviderCallProvenance.OUTCOMES` tuple) recorded on every failure route — transport errors, malformed payloads, unparseable JSON, OpenAI's previously-raw `IndexError` on empty choices now surfaces as `ProviderError`. DeepSeek's previously-lying paths fixed: a failed direct attempt no longer keeps the optimistic `completed_direct` marker (records `failed_direct`), and a 402 with the fallback unconfigured also records `failed_direct` instead of presenting requested-as-actual; fallback success/failure keep `completed_fallback`/`failed_fallback`. Comparison refusal enforced: `rws eval-compare --strict` now exits 1 when execution conditions differ **or are unknown** (legacy baseline predating `execution_conditions` refuses too), `eval-suite` compare rendering unchanged-loud; the multi-suite leaderboard flags per-row conditions (`clean`/`fallback — do not pool`/`unknown`) and prints a `Pooling verdict: NO` line when any suite is not clean. Legacy compatibility preserved: default provenance still serializes, provider-log entries without the block still load, and `ci-eval-gate.py`'s gold-baseline comparison keeps its regression-only strict semantics (the shipped `evals/baselines/gold.json` predates execution conditions by design and must not start failing CI). No paid live calls — all evidence injected offline. Evidence: new shared contract suite [`tests/test_provider_provenance_contract.py`](https://github.com/gasyoun/RuWritingStyles/blob/main/tests/test_provider_provenance_contract.py) (adapter/outcome inventory mapped to tests, direct + success-fallback + failed-fallback + missing-usage + legacy/unknown receipts, CLI strict-refusal and leaderboard verdict tests); gates: `compileall` OK, `validate_project.py` SUCCESS, full `pytest -q` **427 passed + 106 subtests** (from 407 + 74).

_Dr. Mārcis Gasūns_
