- **NKRYa keyness in eight style passports and advisory corpus evidence in scrutiny (H5283, Claude Code Opus 5.5 (`claude-opus-5-5`), 23-09-2026):** the four Zaliznyak passports went first (`imennoe`, `novgorod`, `enklitiki`, `udarenie`), then Bartold, Turaev, Krachkovskij and Golenishchev. Each one now has a measured «Лексическая подпись (НКРЯ)» table: the top 30 content lemmas of its source text, each with its Russian National Corpus ipm and band, ranked by log₂ keyness. For example, Enklitiki's top lemma is `энклитика` +17.5. The tables are generated by [`scripts/nkrya_keyness.py`](https://github.com/gasyoun/RuWritingStyles/blob/main/scripts/nkrya_keyness.py), and their reports are in [`metadata/nkrya_keyness/`](https://github.com/gasyoun/RuWritingStyles/tree/main/metadata/nkrya_keyness). The 185 cached API responses are in `metadata/nkrya_cache/`. The lemmatiser handles pre-reform spelling and strips the `\x01`/`\x02` stress bytes the Ударение-2019 extraction left inside words. It also skips sigla and abbreviations, which had made `чудо`, `регулы` and `табла` the text's "key words". `rws scrutiny --nkrya offline|live` adds an NKRYa evidence table for archaic-looking words to the prompt: ipm across all periods, ipm in the 1800–1899 slice, and a period-skew hint. Executed `anachronism` findings get the same evidence as `nkrya_evidence` for their new optional `term` field; severity and confidence are never changed. The new module is [`src/ruwritingstyles/nkrya_evidence.py`](https://github.com/gasyoun/RuWritingStyles/blob/main/src/ruwritingstyles/nkrya_evidence.py). It reads the cache offline with no token or client, so CI stays offline. Live fetches go through csl-pyutil's shared client once H5282 lands, and until then through SanskritLexicography's client by path. The new optional extra `nkrya` (pymorphy3) is installed by CI. The prompt changes are folded into the existing "Baseline owed on the next PAID run" item in `.ai_state.md`, so no separate paid run is needed. Method and caveats: [docs/NKRYA_KEYNESS_STYLE_PASSPORTS_H5283_23-09-2026.md](https://github.com/gasyoun/RuWritingStyles/blob/main/docs/NKRYA_KEYNESS_STYLE_PASSPORTS_H5283_23-09-2026.md). Gates: `compileall` OK, `validate_project.py` SUCCESS, `pytest -q` **441 passed + 114 subtests**, `ci-eval-gate.py` no regressions.

_Гасунс_
