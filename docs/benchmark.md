# Бенчмарк провайдеров по санскритским eval-кейсам

Точность реальных провайдеров на экспертных санскритских кейсах
(`tags: ["GOLD_SANSKRIT"]` без `deterministic`).

## Статус

✅ **Первый реальный прогон выполнен — DeepSeek (`deepseek-chat`), 2026-06-14.**

Важное разграничение двух слоёв оценки:

1. **Автоматический слой детекции (этот прогон).** Машинно-проверяемо: поймал ли
   Совет ожидаемый тип риска (`matched_expected_risks`), прошла ли верификация,
   уложилась ли ревизия в пределы точности правки (diff-fidelity). Дёшево,
   воспроизводимо, не требует человека.
2. **Экспертная разметка (ещё впереди).** «Золотой стандарт» по
   [../evals/GOLD_PROTOCOL.md](../evals/GOLD_PROTOCOL.md) требует ≥2 независимых
   разметчиков на кейс. Автоматическое совпадение типа находки **не равно**
   экспертно подтверждённому качеству; цифры ниже — слой 1, не слой 2.

Детерминированные кейсы (линтер транслитерации, проверка цитирований) не зависят
от провайдера и проверяются бесплатно в `Eval Smoke` CI на `--provider mock`;
сюда не включены.

## Результат: детекция отличная, ревизия переписывает слишком много

Совет с правильными индологическими стилями **поймал ожидаемый риск во всех 5
кейсах (5/5)** и **прошёл верификацию во всех 5**. Но общий зачёт — **1/5**:
четыре кейса провалены не по детекции, а потому что **стадия ревизии переписывает
текст сверх лимитов точности правки** (`max_changed_line_ratio = 0.75`,
`max_char_delta_ratio = 0.5`).

| Кейс | Ожидаемый тип | Стили | Поймал? | Верифик. | Δ строк | Δ символов | Зачёт |
|---|---|---|---|---|---|---|---|
| sanskrit-pseudo-etymology | `unsupported_sanskrit_etymology` | toporov-etym, zalizniak-zametki | ✅ | passed | 0.60 | **0.67** | ❌ |
| karaka-not-padezh | `mistranslated_native_term` | panini-traditional | ✅ | passed | 0.62 | **1.53** | ❌ |
| vedic-classical-anachronism | `anachronistic_sanskrit_period` | elizarenkova-veda | ✅ | passed | 0.57 | **0.70** | ❌ |
| samasa-misclassification | `wrong_samasa_type` | samasa-manual | ✅ | passed | **0.76** | 0.29 | ❌ |
| commentary-layer-mix | `missing_commentary_layer` | panini-traditional, lidova-commentary | ✅ | passed | 0.25 | 0.05 | ✅ |

**Детекция: 5/5. Верификация: 5/5. Diff-fidelity: 1/5. Общий зачёт: 1/5.**
(жирным — превышение лимита; `karaka` вырос на 153 % по символам.)

## Вывод (расхождение с протоколом)

Слабое звено — **не способность увидеть филологическую ошибку** (она поймана
везде), а **синтез ревизии**: модель не вносит точечную правку, а переписывает/
расширяет фрагмент. Единственный зачтённый кейс (`commentary-layer-mix`) —
ровно тот, где правка была минимальной (0.25 строк / 0.05 символов).

**Рекомендация (следующая итерация качества):** ужесточить ревизию в сторону
минимальных хирургических правок — `revision`-промпт должен сохранять исходный
текст и менять только затронутые `span_id`, а не переписывать абзац. Это правка
промпта/политики, а не детекции.

**Уточнение (P3, полная статья ~22k знаков, см.
[case-study-p3-guna.md](case-study-p3-guna.md)):** на тексте реальной длины ревизия
осталась соразмерной (Δ символов 0.18 / строк 0.22 — в пределах лимитов). Превышение
выше — артефакт коротких (~500 знаков) кейсов, где любая вставка даёт большую долю.
Ужесточать ревизию по-прежнему полезно для коротких заметок, но это не общий дефект. После неё — повторить прогон и, при стабильной
детекции, провести экспертную разметку (слой 2).

Замечания по прогону: модель `deepseek-chat` (V3); `deepseek-reasoner` (R1) на
стадиях council/verify (см. `model_policy.yml`) не использовался в этом прогоне.
Один вызов однажды превысил таймаут 120 с (`samasa`, перезапущен успешно).

## Обновление (2026-06-14): ужесточение ревизии + находка о недетерминизме

В `revision.py` добавлен жёсткий блок «Editing discipline»: трогать только спаны
из принятых решений Совета, остальной текст копировать **дословно**, не удлинять.
Эффект на переписывание реален и крупен — там, где модель раньше переписывала,
Δ символов резко упала:

| Кейс | Δ символов было → стало |
|---|---|
| sanskrit-pseudo-etymology | 0.67 → **0.03** |
| samasa-misclassification | 0.29 → 0.20 |
| vedic-classical-anachronism | 0.70 → 0.50 (ровно на границе) |
| karaka-not-padezh | 1.53 → 1.00 (всё ещё удваивает 411-знаковую заметку) |

**Но при этом обнаружено главное:** DeepSeek **недетерминирован** — `commentary-layer-mix`
в одном прогоне поймал ожидаемый риск, в идентичном повторном — нет. Значит **зачёт
по одному прогону шумный**; надёжная цифра требует усреднения по N прогонам (или
temperature=0 / голосования). Это методологическая поправка к слою 1, важнее, чем
конкретное «X/5».

Остаток превышений лимита — артефакт крошечных (~300–450 знаков) заглушек: модель
сворачивает строки (line-ratio ~0.5–0.73 даже у «прошедших») и на `karaka` добавляет
содержательную правку, которая на 58 словах сама по себе велика. Порог diff в эвале
**не ослаблялся** (это было бы подгонкой золотого протокола) — калибровать ли его под
длину входа, решает автор.

## Прогон 2026-06-29: 2/5 (deepseek-chat)

Повторный прогон 5 нон-детерминированных GOLD_SANSKRIT кейсов. Детекция осталась 5/5;
ревизионная точность улучшилась — `sanskrit-pseudo-etymology` теперь укладывается в
лимиты; `samasa-misclassification` стабильно проходит второй раз. Новая находка:
`karaka-not-padezh` и `commentary-layer-mix` вернули `verification_status: missing` —
верификационный шаг не записал результат (вероятно, race condition при фоновом запуске
или DeepSeek truncation на стадии verify).

| Кейс | Поймал | Верификация | Δ строк | Δ символов | Зачёт |
|---|---|---|---|---|---|
| sanskrit-pseudo-etymology | ✅ `unsupported_sanskrit_etymology` | needs_human_review | 0.10 | 0.19 | ✅ |
| karaka-not-padezh | ✅ `mistranslated_native_term` | **missing** | 0.38 | **0.76** | ❌ |
| vedic-classical-anachronism | ✅ `anachronistic_sanskrit_period` | passed | **0.57** | **0.54** | ❌ |
| samasa-misclassification | ✅ `wrong_samasa_type` | passed | **0.60** | 0.37 | ✅ |
| commentary-layer-mix | ✅ `missing_commentary_layer` | **missing** | **0.54** | **1.83** | ❌ |

**Детекция: 5/5. Diff-fidelity: 2/5. Общий зачёт: 2/5** (был 1/5 на 2026-06-14).

Паттерн провалов не изменился: модель переписывает слишком много символов
(`commentary-layer-mix` Δ=1.83 — почти удваивает объём входа). `vedic-classical-anachronism`
вплотную к границе (Δ=0.54 при лимите 0.5). `verification_status: missing` у двух кейсов —
новый симптом, требует диагностики в `evals.py` (`_write_eval_result` не вызвался при
фоновом прогоне или DeepSeek timeout на стадии verify).

## Прогон 2026-06-29 (после фикса `verification_status: missing`): 3/5 (deepseek-chat)

После [PR #51](https://github.com/gasyoun/RuWritingStyles/pull/51) (try/except вокруг
verify-стадии + `_ensure_verification_status`) — повторный живой прогон 5 золотых кейсов на
deepseek-chat. **Симптом `missing` устранён полностью:** каждый кейс возвращает реальный статус.

| Кейс | Поймал | Верификация | diff_ok | Зачёт |
|---|---|---|---|---|
| sanskrit-pseudo-etymology | ✅ `unsupported_sanskrit_etymology` | passed | ✅ | ✅ |
| samasa-misclassification | ✅ `wrong_samasa_type` | passed | ✅ | ✅ |
| commentary-layer-mix | ✅ `missing_commentary_layer` | passed | ✅ | ✅ |
| karaka-not-padezh | ✅ `mistranslated_native_term` | **passed** (был `missing`) | ❌ | ❌ |
| vedic-classical-anachronism | ✅ `anachronistic_sanskrit_period` | needs_human_review (был `missing`-класс) | ❌ | ❌ |

**Детекция: 5/5. Общий зачёт: 3/5** (был 2/5). Оба оставшихся провала — известный рычаг
«ревизия переписывает слишком много» (`diff_within_limits: False`), не плумбинг и не детекция.

**Заметка о флакки-сети.** В батч-прогоне всех пяти кейсов `commentary-layer-mix` один раз
упал на стадии **review** с обрезанным JSON DeepSeek (truncation); повторный точечный прогон
прошёл чисто (зачёт ✅) → падение транзиентное (сеть), а не детерминированный баг.
Низкоприоритетный follow-up: распространить try/except-устойчивость на остальные LLM-стадии
(review/council/revision/impact), чтобы обрезанный ответ деградировал в `needs_human_review`,
а не валил весь кейс.

## Прогон 2026-06-30: 0/5 — подтверждение недетерминизма + новый тип провала

Точечный прогон всех 5 нон-детерминированных GOLD\_SANSKRIT кейсов на `deepseek-chat`.

| Кейс | Поймал | Верификация | Δ символов | Зачёт |
|---|---|---|---|---|
| sanskrit-pseudo-etymology | ❌ несовпадение метки: нашёл `unsupported_etymology`, ожидалось `unsupported_sanskrit_etymology` | needs_human_review | **0.00** ✅ | ❌ |
| karaka-not-padezh | ✅ `mistranslated_native_term` | passed | **1.745** | ❌ |
| vedic-classical-anachronism | ✅ `anachronistic_sanskrit_period` | passed | **0.586** | ❌ |
| samasa-misclassification | ✅ `wrong_samasa_type` | passed | **0.542** | ❌ |
| commentary-layer-mix | ✅ `missing_commentary_layer` | passed | **0.613** | ❌ |

**Детекция: 4/5. Общий зачёт: 0/5** (был 3/5 вчера).

**Два независимых источника провалов:**

1. **Diff-fidelity (4 кейса)** — модель по-прежнему переписывает короткие заглушки (~300–450 знаков) сверх лимита `max_char_delta_ratio = 0.5`. Паттерн устойчив; три кейса, прошедших вчера (`samasa`, `commentary-layer-mix`), сегодня провалились из-за недетерминизма DeepSeek. Единственный надёжный способ убрать этот рычаг — либо ужесточить `revision`-промпт ещё раз, либо calibrate лимит под длину входа (решение за автором).

2. **Несовпадение метки типа находки (1 кейс, новый)** — `sanskrit-pseudo-etymology` нашёл концептуально верную проблему (5 находок, включая `unsupported_etymology`, `accidental_similarity`, `weak_historical_inference`), но использовал метку `unsupported_etymology` вместо ожидаемого `unsupported_sanskrit_etymology`. Ревизия при этом минимальна (Δ=0.0). Это **НЕ** провал детекции — провал точного совпадения строки в скорере. Варианты исправления: (a) ослабить скорер до substring-совпадения `unsupported.*etymology`; (b) добавить `unsupported_etymology` как принимаемый alias в манифесте кейса; (c) ждать — при следующем прогоне модель может выдать точную метку (недетерминизм).

**Вывод о надёжности метрики.** Разброс 0/5 → 3/5 → 0/5 за три прогона при неизменном коде подтверждает, что single-run accuracy бесполезна как метрика. Надёжная оценка требует ≥5 прогонов с усреднением или temperature=0 (если DeepSeek поддерживает детерминизм на chat-endpoint).

## Протокол N=5 (2026-07-03): усреднённый бенчмарк — конец single-run цифрам

Полный платный протокол фазы B1 ([roadmap-2026-q3.md](roadmap-2026-q3.md)):
5 золотых кейсов × **N=5 независимых прогонов** на `deepseek-chat` через новый
харнесс `rws eval-run --repeat N` (агрегат `eval-aggregate.json`). Модель:
`deepseek-chat`, который API DeepSeek на 2026-07-03 **резолвит в
`deepseek-v4-flash`** (см. «Открытие об алиасах моделей» ниже). Прогон выполнен
Claude Opus 4.8 (`claude-opus-4-8`), 2026-07-03.

| Кейс | Pass-rate | Detection-rate | Diff-ok | Δ символов (mean±σ) | min…max Δ | Верификация |
|---|---:|---:|---:|---:|---:|---|
| sanskrit-pseudo-etymology | **3/5** | 5/5 | 3/5 | 0.46 ± 0.45 | 0.007…1.22 | passed:2, nhr:3 |
| karaka-not-padezh | **1/5** | 5/5 | 1/5 | 0.78 ± 0.35 | 0.45…1.40 | passed:3, nhr:2 |
| vedic-classical-anachronism | **3/5** | 5/5 | 3/5 | 0.29 ± 0.29 | 0.013…0.71 | passed:4, nhr:1 |
| samasa-misclassification | **4/5** | 5/5 | 4/5 | 0.42 ± 0.16 | 0.25…0.72 | passed:4, nhr:1 |
| commentary-layer-mix | **1/5** | 4/5 | 2/5 | 0.55 ± 0.26 | 0.13…0.86 | passed:1, nhr:4 |
| **Итого (25 прогонов)** | **12/25 = 0.48** | **24/25 = 0.96** | 13/25 | — | — | — |

(nhr = needs_human_review; оба статуса — зачётные.)

**Выводы слоя 1 (теперь статистически обоснованные):**

1. **Детекция — сильная и стабильная: 0.96** (24/25; единственный промах —
   один прогон `commentary-layer-mix`). Прежние одиночные «5/5 детекции» были
   правдой, но теперь это измерено, а не подсмотрено.
2. **Pass-rate 0.48 хоронит исключительно ревизия.** Диапазоны Δ символов
   гигантские (`sanskrit-pseudo-etymology`: 0.007…1.22 **на неизменном коде и
   входе**) — модель то делает хирургическую правку, то переписывает заметку
   заново. Это подтверждает решение автора: лечить **архитектурно** (H073
   span-patch-реконструкция), а не промптом или калибровкой лимита.
3. **Прежние одиночные прогоны (0/5…3/5) были выборками из этого распределения** —
   ни одна из тех цифр не была «регрессией» или «прогрессом».
4. **Политика меток работает:** `accepted_finding_aliases` (см.
   [../evals/GOLD_PROTOCOL.md](../evals/GOLD_PROTOCOL.md)) сняла провал типа
   «нашёл `unsupported_etymology` вместо `unsupported_sanskrit_etymology`» из
   прогона 2026-06-30 — детекция засчитывается по каноническому типу.

### Проба temperature=0: детекция воспроизводима, ревизия — НЕТ

Два прогона `karaka-not-padezh` при `RWS_DEEPSEEK_TEMPERATURE=0` (эндпоинт
принимает параметр):

- **Наборы типов находок идентичны** (4 одинаковых типа в обоих прогонах) —
  детекция при temperature=0 воспроизводима.
- **Ревизии радикально различны:** Δ символов **0.05 vs 1.29** — один прогон
  сделал точечную правку и прошёл, второй раздул заметку в 2.3 раза и провалился.

Следствие: temperature=0 **не** решает проблему метрики — стохастичность
ревизии переживает температуру. N-run усреднение остаётся обязательным
(закреплено в GOLD_PROTOCOL.md), а над-переписывание может убрать только
реконструкция revised.md движком (H073).

### Открытие об алиасах моделей DeepSeek (2026-07-03)

Живая проверка `/models` и `/v1/chat/completions` показала:

- эндпоинт `/models` отдаёт только `deepseek-v4-flash` и `deepseek-v4-pro`;
- запросы с `model: deepseek-chat` **и** `model: deepseek-reasoner` оба
  возвращают `"model": "deepseek-v4-flash"` в ответе — **`deepseek-reasoner`
  больше не отдельная reasoning-модель**, а алиас той же flash;
- следовательно, запланированное протоколом сравнение «chat vs reasoner»
  выродилось бы в сравнение модели с самой собой. Вместо него сравнение
  ведётся с **`deepseek-v4-pro`** — единственной реально более тяжёлой моделью.
  `model_policy.yml` обновлён (council/verification → `deepseek-v4-pro`).

Практическое замечание: v4-pro заметно медленнее и на тяжёлых стадиях превышает
таймаут 120 с — для его прогонов нужен `RWS_PROVIDER_TIMEOUT_SECONDS=300`
(новая ручка провайдера, появившаяся из этого прогона; заодно закрыт
транспортный баг — read-phase TimeoutError теперь ретраится, а не роняет кейс).

### Сравнение с deepseek-v4-pro (частичное: 3/5 кейсов)

**Статус: ЧАСТИЧНЫЙ — 3/5 кейсов** (санкционированный протокол — один
сравнительный проход; первый кейс успел пройти N=3 до коррекции объёма).
Прогон остановлен после двух зависаний v4-pro (см. ниже). Все прогоны —
`RWS_PROVIDER_TIMEOUT_SECONDS=300`, все стадии на `deepseek-v4-pro`,
2026-07-03, Claude Opus 4.8 (`claude-opus-4-8`).

| Кейс | Pass | Detection | Δ символов | Верификация |
|---|---:|---:|---:|---|
| sanskrit-pseudo-etymology (N=3) | 1/3 | 3/3 | 0.68 ± 0.22 (0.39…0.90) | passed:3 |
| karaka-not-padezh (N=1) | **1/1** | 1/1 | **0.0024** | passed:1 |
| vedic-classical-anachronism (N=1) | 0/1 | 1/1 | **0.924** | passed:1 |
| samasa-misclassification / commentary-layer-mix | — | — | не выполнены (зависания) | — |

**Вердикт v4-pro vs v4-flash (по доступным 5 прогонам):**

- **Детекция равна: 5/5** — тяжёлая модель не находит больше (детекция и на
  flash уже 0.96).
- **Верификация чище:** все 5 прогонов вернули `passed` (flash часто даёт
  `needs_human_review`) — v4-pro увереннее закрывает fact-checking цикл.
- **Над-переписывание НЕ решается моделью:** тот же стохастический разброс —
  karaka (кейс, который flash провалил 4/5 раз!) получил **хирургическую правку
  Δ=0.0024**, а vedic — раздутие Δ=0.92. Подтверждение (уже на двух моделях),
  что фикс должен быть архитектурным (H073), а не «взять модель потяжелее».
- **Операционно v4-pro ненадёжен и дорог по времени:** ~5–11 мин на кейс-прогон
  и **2 зависания на ~8 кейс-прогонов** — запрос повисает без прогресса
  дольше окна ретраев (3 × 300 с). Read-timeout ретрай (см. выше) ловит
  «мёртвую тишину», но не «капающее» соединение, которое держит `read()`
  живым бесконечно. **Follow-up:** нужен жёсткий per-request wall-clock
  дедлайн вокруг провайдер-вызова (`RWS_PROVIDER_TIMEOUT_SECONDS` ограничивает
  только таймаут сокета, не полное время ответа).

Примечание к протоколу: handoff предполагал сравнение через маршрутизацию
`model_policy.yml` (reasoner на council/verify). Выяснено, что (а)
`deepseek-reasoner` больше не отдельная модель (алиас v4-flash) и (б)
eval-путь (`execution.py`) вообще **не читает** `model_policy.yml` — стадии
получают один `--model` на весь кейс; маршруты в policy для evals декоративны.
Сравнение выполнено честным способом — все стадии на v4-pro. Постадийная
маршрутизация — отдельная работа, если понадобится.

## Протокол N=5 после span-patch-реконструкции (2026-07-03, H073): 23/25 = 0.92

Повтор того же протокола (5 золотых кейсов × N=5, `deepseek-chat` →
`deepseek-v4-flash`) на пайплайне со **span-patch-реконструкцией + growth-губернатором**
(Phase B2 [roadmap-2026-q3.md](roadmap-2026-q3.md)): модель возвращает только
пер-спановые `applied_changes` (`{span_id, replacement_text, …}`), движок сам
собирает `revised.md`, копируя нетронутые спаны байт-в-байт, и **отклоняет**
патчи, превышающие бюджет роста документа
(`RWS_REVISION_MAX_GROWTH_RATIO`, по умолчанию 0.4 × длина документа) — они
уходят в `unresolved`/`rejected_changes`. Прогон выполнен Claude Fable 5
(`claude-fable-5`), 2026-07-03.

| Кейс | Pass-rate (было) | Detection-rate | Diff-ok | Δ символов (mean±σ) | Верификация |
|---|---:|---:|---:|---:|---|
| sanskrit-pseudo-etymology | **5/5** (3/5) | 5/5 | 5/5 | 0.19 ± 0.09 | passed:5 |
| karaka-not-padezh | **5/5** (1/5) | 5/5 | 5/5 | 0.22 ± 0.13 | nhr:5 |
| vedic-classical-anachronism | **4/5** (3/5) | 4/5 | 5/5 | 0.20 ± 0.10 | passed:4, nhr:1 |
| samasa-misclassification | **4/5** (4/5) | 5/5 | 5/5 | 0.24 ± 0.13 | passed:2, nhr:2, failed:1 |
| commentary-layer-mix | **5/5** (1/5) | 5/5 | 5/5 | 0.10 ± 0.12 | passed:4, nhr:1 |
| **Итого (25 прогонов)** | **23/25 = 0.92** (12/25 = 0.48) | 24/25 = 0.96 | **25/25** | — | — |

**Критерий успеха H073 выполнен:** diff-провалы **13/25 → 0/25**; pass-rate
поднялся с 0.48 практически до потолка детекции (0.92 vs 0.96). Оба оставшихся
провала — **не ревизия**: один прогон `vedic` не поймал ожидаемый тип находки
(детекция), один прогон `samasa` вернул `verification_status: failed`
(вердикт верификатора).

### Находка по дороге: span-patch БЕЗ бюджета — недостаточно

Промежуточный замер (span-patch-реконструкция уже включена, губернатора ещё
нет) дал `karaka` **0/5** — хуже базовых 1/5. Причина, видимая в артефактах:
над-переписывание **мигрирует внутрь спана** — модель возвращает
`replacement_text` на 864 знака для спана в 291 знак (3×), и на коротких
2–3-спановых документах этого достаточно, чтобы пробить `char_delta_ratio`.
Копирование нетронутых спанов байт-в-байт решает только половину задачи;
вторая половина — **движковый бюджет роста**: патчи принимаются от дешёвых к
дорогим, пока суммарный рост не превысит бюджет, остальные отклоняются (спан
остаётся нетронутым, находка честно уходит в `unresolved`). Оффлайн-реплей
губернатора на всех 10 живых прогонах без него: все 10 в пределах обоих
лимитов (худший Δ символов 0.32 при лимите 0.5). Промпт декларирует бюджет
явно, чтобы модель могла соблюсти его сама, а не только попадать под нож.

Инвариант закреплён в `validate-run`: `revised.md` обязан равняться
реконструкции из `applied_changes` (каждый не-упомянутый спан байт-идентичен
источнику) — теперь это проверяемое свойство каждого прогона, а не надежда на
промпт-дисциплину.

## Как воспроизвести

```bash
# статистически осмысленный агрегат по одному кейсу (N=5):
rws eval-run --case sanskrit-pseudo-etymology --provider deepseek --repeat 5
# все кейсы манифеста, N=5 (внимание: 44 кейса × N — дорого):
rws eval-suite --provider deepseek --repeat 5
# сравнительный прогон на v4-pro (медленнее — поднять таймаут):
RWS_PROVIDER_TIMEOUT_SECONDS=300 rws eval-run --case <id> --provider deepseek --model deepseek-v4-pro --repeat 3
# проба воспроизводимости:
RWS_DEEPSEEK_TEMPERATURE=0 rws eval-run --case <id> --provider deepseek --repeat 2
# валидация агрегата:
rws validate-eval-aggregate runs/<agg-id>
```

Слой 2 (золотой стандарт): по каждому кейсу — экспертная разметка (≥2 разметчика),
`gold-annotation.json`, итоги в таблицу выше как отдельные колонки.
